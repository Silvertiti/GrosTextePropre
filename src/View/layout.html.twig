<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{% block title %}Plateforme de Stages - Accueil{% endblock %}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/style.css" />
    <style>
    /* ----- NAVBAR ----- */
    .navbar {
        background-color: #ff6b6b !important; /* Rouge pétant */
        margin-bottom: 0;
    }

    .navbar-brand {
        font-size: 24px;
        font-weight: bold;
        color: white !important;
    }

    .nav-link {
        color: white !important;
    }

    .nav-link:hover {
        color: #ffc107 !important; /* Jaune Bootstrap */
    }

    /* Pour s’assurer que le body prend bien toute la hauteur */
    html, body {
        height: 100%;
        margin: 0;
        padding: 0;
    }

    body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
    }

    main {
        flex: 1;
    }

    /* Footer déjà vu */
    footer {
        background-color: #ff6b6b;
        color: white;
        text-align: center;
        padding: 20px 0;
        width: 100%;
        position: sticky;
        bottom: 0;
    }

    .footer-bottom {
        border-top: 1px solid #ff6b6b;
        padding-top: 10px;
        margin-top: 10px;
        font-size: 13px;
    }

    {% block style %}{% endblock %}
    </style>
  </head>
  <body>
    {% include 'partials/header.html.twig' %}

    <main>
      {% block content %}{% endblock %}
    </main>

    {% include 'partials/footer.html.twig' %}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>

</html>
